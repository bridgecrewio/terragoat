apiVersion: batch/v1
kind: Job
metadata:
  name: pi-4
spec:
  template:
    spec:
      containers:
      - name: pi
        image: perl
        command: ["perl",  "-Mbignum=bpi", "-wle", "print bpi(4 + 1000)"]
      restartPolicy: Never
